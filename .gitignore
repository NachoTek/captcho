# ── Convenience batch files ──
*.bat

# ── .NET build outputs ──
[Bb]in/
[Oo]bj/
*.user
*.suo

# ── Native DLLs (built from Rust, copied to runtimes/) ──
captcho-capture/runtimes/

# ── Rust build outputs ──
rust-dll/target/

# ── Misc scripts (root level only) ──
/*.ps1

# ── Skill working state (ephemeral; ticket briefs live on the issue tracker) ──
.scratch/

# ── GSD baseline (auto-generated) ──
.gsd
.gsd-id
.mcp.json
.bg-shell/
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
.env
.env.*
!.env.example
node_modules/
.next/
dist/
build/
__pycache__/
*.pyc
.venv/
venv/
target/
vendor/
*.log
coverage/
.cache/
tmp/

# ── GSD baseline (auto-generated) ──
nul
nul.*
con
con.*
prn
prn.*
aux
aux.*
com[1-9]
com[1-9].*
lpt[1-9]
lpt[1-9].*
